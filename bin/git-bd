#!/usr/bin/env bash

# shellcheck disable=SC2016
git branch | sed 's/^[* ]*//' | fzf --multi \
  --preview 'git diff $(git merge-base HEAD {})..{} | delta' \
  --bind 'ctrl-d:preview-half-page-down,ctrl-u:preview-half-page-up' \
  | xargs -r git branch -D

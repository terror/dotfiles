#!/bin/bash

function abs_path() {
  (cd "$(dirname "$1")" &>/dev/null && printf "%s/%s" "$PWD" "${1##*/}")
}

function main() {
  open -na "Google Chrome" --args --incognito "$(abs_path "$1")"
}

main "$@"

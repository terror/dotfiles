#!/usr/bin/env bash

set -euxo pipefail

brew evolve

rustup update

install-crates

kondo "$HOME/src" --all

rsync -auv --delete "$HOME/src/" "$DROPBOX/src"
rsync -auv --delete "$HOME/work/" "$DROPBOX/work"

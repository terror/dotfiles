#!/usr/bin/env bash

set -euo pipefail

: "${NOTE_DIR:?NOTE_DIR is not set}"

today="$(date +%F)"

note_file="$NOTE_DIR/$today.md"

mkdir -p "$NOTE_DIR"

touch "$note_file"

"${EDITOR:-vi}" "$note_file"
